import { SortedProductPipe } from './sorted-product.pipe';

describe('SortedProductPipe', () => {
  it('create an instance', () => {
    const pipe = new SortedProductPipe();
    expect(pipe).toBeTruthy();
  });
});
