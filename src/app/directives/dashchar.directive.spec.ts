import { DashcharDirective } from './dashchar.directive';

describe('DashcharDirective', () => {
  it('should create an instance', () => {
    const directive = new DashcharDirective();
    expect(directive).toBeTruthy();
  });
});
